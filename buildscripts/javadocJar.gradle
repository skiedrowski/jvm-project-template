// custom task for creating a javadoc jar
task javadocJar(type: Jar, dependsOn: javadoc) {
	classifier = 'javadoc'
	from javadoc.destinationDir
}

// add javadoc jar task as artifact
artifacts {
	archives javadocJar
}
